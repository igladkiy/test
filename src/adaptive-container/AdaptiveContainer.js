import React from 'react';
import './AdaptiveContainer.css'; 

const AdaptiveContainer = ({ children }) => {
  return <div className="adaptive-container">{children}</div>;
};

export default AdaptiveContainer;
